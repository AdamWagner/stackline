#!/usr/local/bin/dash

/usr/local/bin/yabai -m query --windows --space \
        | /usr/local/bin/jq --raw-output --compact-output --monochrome-output  'map({"\(.id)": .["stack-index"]}) | reduce .[] as $item ({}; . + $item)'


# WIN_ID="46041"
# WIN_ID="$1"

# IDX=$(/usr/local/bin/yabai -m query --windows --window "$WIN_ID" \
#         | /usr/local/bin/jq --raw-output --compact-output --monochrome-output '.["stack-index"]')

# /Users/adamwagner/Programming/dotfiles/scripts/notify "IDX for $WIN_ID IS $IDX"

# echo -n "$IDX"
