
return {
  testGen = require 'spec.helpers.schemas.test_generators',  -- object getters & schema that generate a busted test for each entity field
  entities = require 'spec.helpers.schemas.entity_schemas',  -- valid.lua schemas test object all at once
}
